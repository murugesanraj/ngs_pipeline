// Copy to conf/priority.config, replace both placeholders, and keep it untracked.
process {
    executor = 'slurm'
    queue = 'REPLACE_WITH_PRIORITY_PARTITION'
    clusterOptions = '--account=REPLACE_WITH_INVESTOR_ACCOUNT'
    scratch = "/local/scratch/${env('USER')}"
}

conda.enabled = true
